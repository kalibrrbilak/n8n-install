#!/bin/bash

# Цвета для красоты
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
PURPLE='\033[0;35m'
CYAN='\033[0;36m'
WHITE='\033[1;37m'
NC='\033[0m' # No Color
BOLD='\033[1m'

# Функция для отображения прогресс-бара
show_progress() {
    local current=$1
    local total=$2
    local width=50
    local percentage=$((current * 100 / total))
    local completed=$((width * current / total))
    local remaining=$((width - completed))

    printf "\r${CYAN}Прогресс: ${NC}["
    printf "%${completed}s" | tr ' ' '█'
    printf "%${remaining}s" | tr ' ' '░'
    printf "] ${BOLD}${GREEN}%3d%%${NC}" $percentage
}

# Красивый баннер
clear
cat << "EOF"
╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║   ███╗   ██╗ █████╗ ███╗   ██╗    ███████╗██╗   ██╗██████╗ ███████╗██████╗  ║
║   ████╗  ██║██╔══██╗████╗  ██║    ██╔════╝██║   ██║██╔══██╗██╔════╝██╔══██╗ ║
║   ██╔██╗ ██║╚█████╔╝██╔██╗ ██║    ███████╗██║   ██║██████╔╝█████╗  ██████╔╝ ║
║   ██║╚██╗██║██╔══██╗██║╚██╗██║    ╚════██║██║   ██║██╔═══╝ ██╔══╝  ██╔══██╗ ║
║   ██║ ╚████║╚█████╔╝██║ ╚████║    ███████║╚██████╔╝██║     ███████╗██║  ██║ ║
║   ╚═╝  ╚═══╝ ╚════╝ ╚═╝  ╚═══╝    ╚══════╝ ╚═════╝ ╚═╝     ╚══════╝╚═╝  ╚═╝ ║
║                                                                              ║
║              🚀 Кастомная сборка с AI/ML + Автоматизация 🚀                  ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝
EOF

echo ""
echo -e "${BOLD}${PURPLE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BOLD}${WHITE}                        👨‍💻 Автор: WebSansay${NC}"
echo -e "${PURPLE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""
echo -e "  ${CYAN}📱 Telegram:${NC}      https://t.me/websansay"
echo -e "  ${BLUE}📢 Канал:${NC}         https://t.me/+p3VDHRpArOc5YzM6"
echo -e "  ${YELLOW}💰 Поддержать:${NC}    https://boosty.to/websansay"
echo ""
echo -e "${PURPLE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""
echo -e "${GREEN}${BOLD}📦 Что входит в сборку:${NC}"
echo -e "  ${WHITE}•${NC} OpenAI, TensorFlow, LangChain ${CYAN}(AI/ML)${NC}"
echo -e "  ${WHITE}•${NC} Telegram, Discord, VK, WhatsApp боты"
echo -e "  ${WHITE}•${NC} FFmpeg, ImageMagick ${CYAN}(медиа)${NC}"
echo -e "  ${WHITE}•${NC} Tesseract OCR ${CYAN}(русский + английский)${NC}"
echo -e "  ${WHITE}•${NC} Chromium + Puppeteer ${CYAN}(автоматизация браузера)${NC}"
echo -e "  ${WHITE}•${NC} MongoDB, Redis драйверы"
echo -e "  ${WHITE}•${NC} И ещё 20+ полезных библиотек!"
echo ""
echo -e "${PURPLE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""
echo -e "${YELLOW}⏱️  Время сборки:${NC} ~15-20 минут (зависит от интернета)"
echo -e "${YELLOW}💾 Размер образа:${NC} ~2-3 GB"
echo ""
echo -e "${BOLD}${GREEN}🚀 Начинаю сборку...${NC}"
echo ""
sleep 3

# Создаём временный файл для логов
BUILD_LOG=$(mktemp)

# Запускаем сборку в фоне
docker compose build --no-cache n8n > "$BUILD_LOG" 2>&1 &
BUILD_PID=$!

# Мониторим прогресс
echo -e "${CYAN}${BOLD}Этапы сборки:${NC}\n"

STEPS=(
  "Скачивание базового образа n8n"
  "Обновление Alpine репозиториев"
  "Установка системных пакетов (264 шт)"
  "Настройка Docker CLI и прав"
  "Установка npm пакетов (30+ шт)"
  "Установка OAuth библиотек"
  "Настройка Puppeteer и Chromium"
  "Финализация образа"
)

TOTAL_STEPS=${#STEPS[@]}
current_step=0

while kill -0 $BUILD_PID 2>/dev/null; do
    # Определяем текущий этап по логам
    if grep -q "transferring dockerfile" "$BUILD_LOG" && [ $current_step -lt 1 ]; then
        current_step=1
    elif grep -q "apk update" "$BUILD_LOG" && [ $current_step -lt 2 ]; then
        current_step=2
    elif grep -q "apk add" "$BUILD_LOG" && [ $current_step -lt 3 ]; then
        current_step=3
    elif grep -q "addgroup" "$BUILD_LOG" && [ $current_step -lt 4 ]; then
        current_step=4
    elif grep -q "npm install -g" "$BUILD_LOG" && [ $current_step -lt 5 ]; then
        current_step=5
    elif grep -q "oauth-1.0a" "$BUILD_LOG" && [ $current_step -lt 6 ]; then
        current_step=6
    elif grep -q "PUPPETEER" "$BUILD_LOG" && [ $current_step -lt 7 ]; then
        current_step=7
    fi

    # Отображаем текущий этап
    if [ $current_step -gt 0 ] && [ $current_step -le $TOTAL_STEPS ]; then
        echo -ne "\r${GREEN}✓${NC} ${STEPS[$((current_step-1))]}"
        if [ $current_step -lt $TOTAL_STEPS ]; then
            echo -ne "\n${YELLOW}⏳${NC} ${STEPS[$current_step]}..."
        fi
    fi

    # Прогресс-бар
    echo -e "\n"
    show_progress $current_step $TOTAL_STEPS
    echo -e "\n"

    sleep 2
done

# Ждём завершения процесса
wait $BUILD_PID
BUILD_EXIT_CODE=$?

echo -e "\n\n"

if [ $BUILD_EXIT_CODE -eq 0 ]; then
    cat << "EOF"
╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║                     ✅ СБОРКА ЗАВЕРШЕНА УСПЕШНО! ✅                          ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝
EOF
    echo ""
    echo -e "${GREEN}${BOLD}Образ собран с полным набором библиотек!${NC}"
    echo ""
    echo -e "${CYAN}Следующие шаги:${NC}"
    echo -e "  1. ${WHITE}docker compose up -d${NC}        ${CYAN}# Запустить n8n${NC}"
    echo -e "  2. ${WHITE}docker logs n8n --tail 30${NC}   ${CYAN}# Проверить логи${NC}"
    echo -e "  3. Открыть в браузере ваш домен"
    echo ""
    echo -e "${PURPLE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
    echo -e "${YELLOW}💡 Полезные ресурсы:${NC}"
    echo -e "  ${CYAN}📱 Вопросы и помощь:${NC}  https://t.me/websansay"
    echo -e "  ${BLUE}📢 Готовые сценарии:${NC}  https://t.me/+p3VDHRpArOc5YzM6"
    echo -e "  ${YELLOW}💰 Донаты:${NC}            https://boosty.to/websansay"
    echo -e "${PURPLE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
    echo ""
else
    echo -e "${RED}${BOLD}❌ Ошибка при сборке!${NC}"
    echo ""
    echo -e "${YELLOW}Показываю последние 30 строк лога:${NC}"
    echo -e "${PURPLE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
    tail -30 "$BUILD_LOG"
    echo -e "${PURPLE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
    echo ""
    echo -e "${CYAN}Полный лог сохранён в:${NC} $BUILD_LOG"
    echo -e "${CYAN}Помощь:${NC} https://t.me/websansay"
    exit 1
fi

# Удаляем временный файл
rm -f "$BUILD_LOG"
