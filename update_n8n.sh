#!/bin/bash
# ============================================================
# Скрипт обновления n8n
# Версия 2.0 - БЕЗ ограничений на запуск
# Может быть запущен напрямую или через Telegram бота
# ============================================================

set -e

# Определение директории скрипта
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

# Цвета для вывода
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

log_info() { echo -e "${BLUE}[INFO]${NC} $(date '+%Y-%m-%d %H:%M:%S') $1"; }
log_success() { echo -e "${GREEN}[OK]${NC} $(date '+%Y-%m-%d %H:%M:%S') $1"; }
log_warning() { echo -e "${YELLOW}[WARN]${NC} $(date '+%Y-%m-%d %H:%M:%S') $1"; }
log_error() { echo -e "${RED}[ERROR]${NC} $(date '+%Y-%m-%d %H:%M:%S') $1"; }

# Загрузка переменных окружения
if [ -f "$SCRIPT_DIR/.env" ]; then
    set -a
    source "$SCRIPT_DIR/.env"
    set +a
fi

# Директория логов
LOG_DIR="$SCRIPT_DIR/logs"
mkdir -p "$LOG_DIR"
LOG_FILE="$LOG_DIR/update_$(date +%Y%m%d_%H%M%S).log"

# Логирование в файл
exec > >(tee -a "$LOG_FILE") 2>&1

# Функция отправки в Telegram
send_telegram() {
    local message="$1"
    if [ -n "$TG_BOT_TOKEN" ] && [ -n "$TG_USER_ID" ]; then
        curl -s -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TG_USER_ID}" \
            -d "text=${message}" \
            -d "parse_mode=Markdown" > /dev/null 2>&1 || true
    fi
}

# ============================================================
# Начало обновления
# ============================================================

log_info "=========================================="
log_info "    Обновление n8n"
log_info "=========================================="

# Получение текущей версии
log_info "Получение текущей версии n8n..."
CURRENT_VERSION=$(docker exec n8n n8n --version 2>/dev/null || echo "unknown")
log_info "Текущая версия: $CURRENT_VERSION"

# Получение последней версии
log_info "Проверка последней версии на GitHub..."
LATEST_VERSION=$(curl -s https://api.github.com/repos/n8n-io/n8n/releases/latest | \
    grep '"tag_name"' | \
    sed -E 's/.*"n8n@([^"]+)".*/\1/' 2>/dev/null || echo "unknown")

if [ "$LATEST_VERSION" = "unknown" ]; then
    # Альтернативный способ получения версии
    LATEST_VERSION=$(curl -s https://registry.npmjs.org/n8n/latest | \
        grep -o '"version":"[^"]*"' | \
        cut -d'"' -f4 2>/dev/null || echo "unknown")
fi

log_info "Последняя версия: $LATEST_VERSION"

# Проверка необходимости обновления
if [ "$CURRENT_VERSION" = "$LATEST_VERSION" ] && [ "$CURRENT_VERSION" != "unknown" ]; then
    log_success "Уже установлена последняя версия n8n ($CURRENT_VERSION)"
    send_telegram "✅ n8n уже обновлён до последней версии $CURRENT_VERSION"
    exit 0
fi

send_telegram "🔄 *Начинаю обновление n8n*

📦 Текущая версия: $CURRENT_VERSION
🆕 Целевая версия: $LATEST_VERSION"

# ============================================================
# Создание резервной копии
# ============================================================

log_info "Создание резервной копии..."
if [ -f "$SCRIPT_DIR/backup_n8n.sh" ]; then
    if "$SCRIPT_DIR/backup_n8n.sh"; then
        log_success "Резервная копия создана"
    else
        log_warning "Не удалось создать резервную копию"
    fi
else
    log_warning "Скрипт бэкапа не найден"
fi

# ============================================================
# Остановка n8n
# ============================================================

log_info "Остановка n8n..."
docker compose stop n8n || docker-compose stop n8n

# ============================================================
# Пересборка образа
# ============================================================

log_info "Пересборка Docker образа n8n..."
log_info "Это может занять несколько минут..."

if docker compose build --no-cache n8n; then
    log_success "Образ пересобран"
elif docker-compose build --no-cache n8n; then
    log_success "Образ пересобран (docker-compose)"
else
    log_error "Ошибка пересборки образа"
    send_telegram "❌ Ошибка пересборки образа n8n"
    exit 1
fi

# ============================================================
# Запуск n8n
# ============================================================

log_info "Запуск n8n..."
if docker compose up -d n8n; then
    log_success "n8n запущен"
elif docker-compose up -d n8n; then
    log_success "n8n запущен (docker-compose)"
else
    log_error "Ошибка запуска n8n"
    send_telegram "❌ Ошибка запуска n8n после обновления"
    exit 1
fi

# ============================================================
# Ожидание запуска
# ============================================================

log_info "Ожидание запуска n8n..."
for i in {1..30}; do
    sleep 2
    if docker exec n8n wget --spider -q http://localhost:5678/healthz 2>/dev/null; then
        log_success "n8n отвечает на healthcheck"
        break
    fi
    echo -n "."
done
echo ""

# ============================================================
# Проверка новой версии
# ============================================================

sleep 5
NEW_VERSION=$(docker exec n8n n8n --version 2>/dev/null || echo "unknown")
log_info "Версия после обновления: $NEW_VERSION"

# ============================================================
# Очистка Docker
# ============================================================

log_info "Очистка Docker..."
docker image prune -f > /dev/null 2>&1 || true
docker builder prune -f > /dev/null 2>&1 || true

# ============================================================
# Очистка системы
# ============================================================

log_info "Очистка системы..."
# Удаление неиспользуемых пакетов
apt-get autoremove -y -qq > /dev/null 2>&1 || true

# Очистка журналов
journalctl --vacuum-time=7d > /dev/null 2>&1 || true

# Удаление старых логов обновления (старше 30 дней)
find "$LOG_DIR" -name "update_*.log" -mtime +30 -delete 2>/dev/null || true

# ============================================================
# Проверка статуса
# ============================================================

STATUS=$(docker ps --filter name=n8n --format "{{.Status}}" 2>/dev/null || echo "unknown")
log_info "Статус контейнера: $STATUS"

if echo "$STATUS" | grep -q "Up"; then
    log_success "=========================================="
    log_success "    Обновление успешно завершено!"
    log_success "=========================================="

    send_telegram "✅ *Обновление n8n завершено!*

📦 Старая версия: $CURRENT_VERSION
🆕 Новая версия: $NEW_VERSION
📊 Статус: $STATUS

🔗 Лог: $LOG_FILE"

    exit 0
else
    log_error "=========================================="
    log_error "    Ошибка: контейнер не запустился"
    log_error "=========================================="

    send_telegram "❌ *Ошибка обновления n8n*

Контейнер не запустился после обновления.
Статус: $STATUS

Проверьте логи:
\`docker logs n8n\`

🔗 Лог обновления: $LOG_FILE"

    exit 1
fi
