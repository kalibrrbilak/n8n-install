# n8n Auto Install v3+

Полная автоматическая установка n8n с PostgreSQL, Redis, pgAdmin, Redis Commander, Traefik и Telegram-ботом для управления.

## 🚀 Возможности

- ✅ Установка n8n 2.0+ в один клик
- ✅ PostgreSQL 16 + Redis 7 для максимальной производительности
- ✅ **pgAdmin 4** - веб-интерфейс для управления PostgreSQL
- ✅ **Redis Commander** - веб-интерфейс для мониторинга Redis
- ✅ Автоматический SSL через Traefik v3.2 + Let's Encrypt
- ✅ Telegram-бот для управления (статус, логи, обновление, бэкапы)
- ✅ Автоматические зашифрованные бэкапы с расписанием
- ✅ Восстановление из бэкапов одной командой
- ✅ Queue Mode для обработки воркфлоу через Redis
- ✅ Docker Engine v29 (актуальная версия)
- ✅ Полная поддержка прокси для внешних запросов

## 📋 Требования

- Ubuntu 22.04 / 24.04 (чистый сервер)
- Минимум 2GB RAM, 20GB диск
- **3 домена**, направленных на сервер:
  - `n8n.example.ru` - для n8n
  - `pgadmin.example.ru` - для pgAdmin
  - `redis.example.ru` - для Redis Commander
- Telegram бот (получить у [@BotFather](https://t.me/BotFather))
- Email для SSL сертификатов

## 🎯 Быстрая установка

```bash
bash <(curl -s https://raw.githubusercontent.com/kalibrrbilak/n8n-install/main/install.sh)
```

Скрипт запросит:
- Домен для n8n
- Домен для pgAdmin
- Домен для Redis Commander
- Email для SSL
- Пароль PostgreSQL
- Telegram Bot Token (опционально)
- Telegram User ID (опционально)

Все остальные пароли будут сгенерированы автоматически.

## 🌐 Компоненты системы

| Компонент | Версия | Описание | Адрес |
|-----------|--------|----------|-------|
| **n8n** | latest | Платформа автоматизации | `https://n8n.example.ru` |
| **PostgreSQL** | 16-alpine | База данных | Внутренняя сеть |
| **pgAdmin** | latest | UI для PostgreSQL | `https://pgadmin.example.ru` |
| **Redis** | 7-alpine | Кэш и очереди | Внутренняя сеть |
| **Redis Commander** | latest | UI для Redis | `https://redis.example.ru` |
| **Traefik** | v3.2 | Reverse proxy + SSL | Внутренняя сеть |
| **Telegram Bot** | Node 20 | Управление сервером | Telegram |

## 🔐 Доступ к сервисам

После установки все пароли сохраняются в `/opt/n8n/.env`:

### n8n
```
URL: https://n8n.example.ru
Первый пользователь создается при первом входе
```

### pgAdmin (PostgreSQL UI)
```
URL: https://pgadmin.example.ru
Email: admin@example.ru (ваш email из установки)
Пароль: автогенерируется (смотрите в .env → PGADMIN_PASSWORD)

PostgreSQL сервер уже добавлен автоматически:
- Host: n8n-postgres
- Database: n8n
- User: n8n
- Password: из .env → POSTGRES_PASSWORD
```

### Redis Commander (Redis UI)
```
URL: https://redis.example.ru
User: admin
Password: автогенерируется (смотрите в .env → REDIS_UI_PASSWORD)
```

## 🤖 Команды Telegram бота

| Команда | Описание |
|---------|----------|
| `/start` или `/help` | Справка по командам |
| `/status` | Статус сервера и всех контейнеров |
| `/logs [N]` | Последние N строк логов n8n (по умолчанию 50) |
| `/update` | Обновить n8n до последней версии |
| `/backups` или `/backup` | Создать резервную копию |
| `/restart` | Перезапустить n8n |
| `/urls` | Показать адреса всех веб-интерфейсов |

## 📁 Структура проекта

```
/opt/n8n/
├── install.sh              # Скрипт установки
├── docker-compose.yml      # Конфигурация контейнеров
├── Dockerfile.n8n          # Кастомный образ n8n
├── .env                    # Конфигурация (пароли, домены)
├── .env.template           # Шаблон конфигурации
├── update_n8n.sh           # Скрипт обновления
├── backup_n8n.sh           # Скрипт создания бэкапа
├── restore_n8n.sh          # Скрипт восстановления
├── configs/
│   └── pgadmin/
│       └── servers.json    # Автоконфигурация pgAdmin
├── bot/
│   ├── bot.js              # Telegram бот
│   ├── Dockerfile          # Образ бота
│   └── package.json        # Зависимости
├── logs/                   # Логи операций
└── backups/                # Резервные копии
```

## 🛠️ Управление системой

### Статус сервисов

```bash
cd /opt/n8n
docker compose ps
```

### Просмотр логов

```bash
# Все сервисы
docker compose logs -f

# Только n8n
docker compose logs -f n8n

# pgAdmin
docker compose logs -f n8n-pgadmin

# Redis
docker compose logs -f n8n-redis
```

### Перезапуск сервисов

```bash
# Перезапуск n8n
docker compose restart n8n

# Перезапуск всех сервисов
docker compose restart

# Остановка всех сервисов
docker compose down

# Запуск всех сервисов
docker compose up -d
```

### Обновление n8n

```bash
cd /opt/n8n
./update_n8n.sh
```

Или через Telegram бота: `/update`

Обновление автоматически:
1. Проверяет наличие новой версии
2. Создает резервную копию
3. Обновляет n8n
4. Перезапускает контейнер
5. Очищает старые образы

### Резервное копирование

#### Создание бэкапа

```bash
cd /opt/n8n
./backup_n8n.sh
```

Или через Telegram бота: `/backups`

Бэкап включает:
- Дамп PostgreSQL базы данных
- Все данные n8n (workflows, credentials, настройки)
- Файл .env с конфигурацией
- Шифрование AES-256-CBC (если задан N8N_ENCRYPTION_KEY)

Бэкапы сохраняются в `/opt/n8n/backups/` и автоматически удаляются через 7 дней.

#### Автоматические бэкапы

Автоматически настраиваются через cron (ежедневно в 2:00 МСК).

Для изменения расписания отредактируйте crontab:

```bash
crontab -e
```

#### Восстановление из бэкапа

```bash
cd /opt/n8n

# Список доступных бэкапов
ls -lh backups/

# Восстановление
./restore_n8n.sh backups/n8n_backup_20250101_020000.tar.gz.enc
```

Скрипт восстановления:
1. Остановит все контейнеры
2. Создаст бэкап текущего состояния (на всякий случай)
3. Расшифрует и распакует выбранный бэкап
4. Восстановит базу данных PostgreSQL
5. Восстановит данные n8n
6. Предложит восстановить .env (опционально)
7. Запустит все контейнеры

## 🔧 Настройка

Все настройки находятся в `/opt/n8n/.env`:

### Основные переменные

```bash
# Домены
DOMAIN=n8n.example.ru
PGADMIN_DOMAIN=pgadmin.example.ru
REDIS_DOMAIN=redis.example.ru

# Email для SSL
EMAIL=admin@example.ru

# PostgreSQL
POSTGRES_USER=n8n
POSTGRES_PASSWORD=<автогенерируется>
POSTGRES_DB=n8n

# pgAdmin
PGADMIN_EMAIL=admin@example.ru
PGADMIN_PASSWORD=<автогенерируется>

# Redis
REDIS_PASSWORD=<автогенерируется>
REDIS_UI_USER=admin
REDIS_UI_PASSWORD=<автогенерируется>

# n8n
N8N_ENCRYPTION_KEY=<автогенерируется 64 символа>
WEBHOOK_URL=https://${DOMAIN}/

# Binary Data Mode (filesystem рекомендуется для производительности)
N8N_BINARY_DATA_MODE=filesystem
N8N_DEFAULT_BINARY_DATA_MODE=filesystem

# Proxy settings для Traefik
N8N_EXPRESS_TRUST_PROXY=true
N8N_TRUSTED_PROXIES=*
N8N_PROXY_HOPS=1

# Внешний прокси (опционально)
PROXY_URL=
NO_PROXY=localhost,127.0.0.1,::1,.local,postgres,redis,pgadmin,traefik,n8n,n8n-postgres,n8n-redis,n8n-pgadmin,n8n-redis-commander,n8n-traefik

# Telegram Bot
TG_BOT_TOKEN=<ваш токен>
TG_USER_ID=<ваш user id>

# Timezone (Екатеринбург)
GENERIC_TIMEZONE=Asia/Yekaterinburg
TZ=Asia/Yekaterinburg

# Queue Mode
EXECUTIONS_MODE=queue
QUEUE_BULL_REDIS_HOST=n8n-redis
QUEUE_BULL_REDIS_PORT=6379
```

После изменения `.env` перезапустите сервисы:

```bash
docker compose down
docker compose up -d
```

## 🔒 Безопасность

### Рекомендации

1. **Регулярные бэкапы**: Включены по умолчанию (ежедневно в 2:00)
2. **Обновления**: Проверяйте обновления n8n командой `/update` в боте
3. **Брандмауэр**: Убедитесь, что открыты только порты 80, 443
4. **SSH**: Настройте вход только по ключу, отключите root-доступ
5. **Мониторинг**: Используйте `/status` в боте для проверки состояния

### Доступ к базам данных

PostgreSQL и Redis НЕ доступны напрямую из интернета:
- PostgreSQL доступен только через pgAdmin UI
- Redis доступен только через Redis Commander UI
- Оба сервиса находятся во внутренней Docker сети

Для прямого подключения используйте SSH tunnel:

```bash
# PostgreSQL
ssh -L 5432:localhost:5432 user@server
# затем подключитесь к localhost:5432

# Redis
ssh -L 6379:localhost:6379 user@server
# затем подключитесь к localhost:6379
```

## 🐛 Решение проблем

### n8n не запускается

```bash
# Проверьте логи
docker compose logs n8n

# Проверьте зависимости
docker compose ps

# Убедитесь что PostgreSQL и Redis работают
docker compose ps n8n-postgres n8n-redis
```

### SSL сертификаты не выдаются

1. Убедитесь, что домены указывают на ваш сервер (DNS A-записи)
2. Проверьте, что порты 80 и 443 открыты
3. Проверьте логи Traefik: `docker compose logs n8n-traefik`

### Бот не отвечает

```bash
# Проверьте логи бота
docker compose logs n8n-bot

# Проверьте переменные окружения
grep TG_ /opt/n8n/.env

# Перезапустите бота
docker compose restart n8n-bot
```

### Проблемы с производительностью

1. Проверьте ресурсы: `docker stats`
2. Увеличьте RAM сервера (минимум 2GB, рекомендуется 4GB)
3. Проверьте место на диске: `df -h`
4. Очистите старые образы: `docker system prune -a`

## 📊 Мониторинг

### Через Telegram бота

Команда `/status` показывает:
- Uptime сервера
- Использование диска и RAM
- Версию n8n
- Статус всех контейнеров

### Через веб-интерфейсы

- **pgAdmin**: Мониторинг запросов, размера БД, активных подключений
- **Redis Commander**: Использование памяти, количество ключей, статистика команд
- **n8n**: Встроенная страница `/healthz` и метрики (если N8N_METRICS=true)

## 🆕 Изменения v3+

### Новые компоненты
- ✅ pgAdmin 4 для управления PostgreSQL
- ✅ Redis Commander для мониторинга Redis
- ✅ Скрипт restore_n8n.sh для восстановления из бэкапов
- ✅ Команда `/urls` в боте для быстрого доступа к интерфейсам

### Улучшения
- ✅ Полная конфигурация .env с всеми переменными
- ✅ Поддержка прокси для внешних запросов n8n
- ✅ Binary Data Mode на filesystem для производительности
- ✅ Правильная настройка trust proxy для Traefik
- ✅ Пароли для Redis с автогенерацией
- ✅ Timezone Екатеринбург по умолчанию
- ✅ Автоконфигурация pgAdmin через servers.json

### Исправления
- ✅ Совместимость Telegram бота с BusyBox/Alpine Linux
- ✅ Исправлена команда `uptime` для Alpine
- ✅ Docker compose команды с явным указанием пути
- ✅ Улучшена обработка ошибок во всех скриптах

### ⚙️ n8n 2.x - Совместимость и Файловая Система

Репозиторий полностью адаптирован под **n8n 2.x** с учётом breaking changes по безопасности:

#### 🔓 Execute Command включён
```env
NODES_EXCLUDE=[]
```
Execute Command и Local File Trigger разрешены (по умолчанию отключены в n8n 2.x).

#### 📂 Две файловые зоны для работы с файлами

**1. Стандартная зона n8n v2** (`/home/node/.n8n-files`)
- Sandbox-зона по умолчанию для Read/Write Binary Files нод
- Рекомендуется для безопасной работы с файлами

**2. Кастомная рабочая папка** (`/data`)
- Для проектных файлов и данных
- Полный доступ на чтение/запись

Обе зоны:
- ✅ Автоматически создаются при установке
- ✅ Правильные права (UID:GID = 1000:1000)
- ✅ Включены в whitelist `N8N_RESTRICT_FILE_ACCESS_TO`
- ✅ Примонтированы как Docker volumes

#### 🎯 Как использовать в нодах

**Read/Write Binary Files:**
```
/home/node/.n8n-files/myfile.pdf  ✅ Работает
/data/project/document.xlsx        ✅ Работает
/tmp/file.txt                      ❌ Доступ запрещён
```

**Execute Command:**
```bash
# Сохранить файл в стандартную зону
echo "test" > /home/node/.n8n-files/output.txt

# Или в рабочую папку проекта
cp file.csv /data/reports/
```

#### ⚠️ Важные ограничения n8n 2.x

1. **Только whitelist-пути**: Доступ разрешён ТОЛЬКО к путям в `N8N_RESTRICT_FILE_ACCESS_TO`
2. **Linux права недостаточны**: Даже с `chmod 777` файл вне whitelist будет недоступен
3. **Не отключайте защиту**: `N8N_RESTRICT_FILE_ACCESS_TO` критичен для безопасности

#### 🔧 Task Runners

```env
N8N_RUNNERS_ENABLED=false  # Выполнение в основном процессе (по умолчанию)
```

- `true` = изолированные процессы (безопаснее, больше памяти)
- `false` = основной процесс (быстрее, меньше потребление ресурсов)

## 📝 Полезные команды

```bash
# Просмотр всех паролей
cat /opt/n8n/.env | grep PASSWORD

# Проверка версий
docker exec n8n n8n --version
docker exec n8n-postgres psql -U n8n -c "SELECT version();"
docker exec n8n-redis redis-cli -a "$REDIS_PASSWORD" INFO server | grep redis_version

# Размер базы данных
docker exec n8n-postgres psql -U n8n -c "SELECT pg_size_pretty(pg_database_size('n8n'));"

# Статистика Redis
docker exec n8n-redis redis-cli -a "$REDIS_PASSWORD" INFO stats

# Резервное копирование вручную
/opt/n8n/backup_n8n.sh

# Обновление системы
apt update && apt upgrade -y

# Очистка Docker
docker system prune -a --volumes
```

## 🔄 Миграция с предыдущих версий

Если у вас установлена старая версия без pgAdmin/Redis Commander:

```bash
cd /opt/n8n

# Создайте бэкап
./backup_n8n.sh

# Остановите контейнеры
docker compose down

# Обновите репозиторий
git pull

# Добавьте новые переменные в .env
nano .env
# Добавьте: PGADMIN_DOMAIN, REDIS_DOMAIN, PGADMIN_PASSWORD,
#           REDIS_PASSWORD, REDIS_UI_PASSWORD

# Создайте конфигурацию pgAdmin
mkdir -p configs/pgadmin
# Скопируйте servers.json из репозитория

# Запустите обновленную конфигурацию
docker compose up -d
```

## 📞 Поддержка

- Issues: [GitHub Issues](https://github.com/kalibrrbilak/n8n-install/issues)
- Telegram: [@websansay](https://t.me/websansay)

## 📜 Лицензия

MIT License - используйте свободно для личных и коммерческих проектов.

## 🙏 Благодарности

Основано на [n8n-beget-install](https://github.com/kalininlive/n8n-beget-install) от kalininlive.

---

**Сделано с ❤️ для сообщества n8n**
